'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
// Extracts nested fields from data
var extractNested = exports.extractNested = function extractNested(data, fields) {
  // If no data, return empty object
  if (!data) {
    return {};
  }

  //  If fields aren't passed, return whole object
  if (!fields) {
    return data;
  }

  if (typeof fields === 'string') {
    fields = [fields];
  }

  // Iterate fields
  var extracted = data;
  Object.keys(fields).map(function (item) {
    var key = fields[item].toLowerCase();
    if (extracted[key]) {
      extracted = extracted[key];
    }
  });

  return extracted;
};